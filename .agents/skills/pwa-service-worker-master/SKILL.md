---
name: pwa-service-worker-master
description: |
  Progressive Web App (PWA) manifest configuration, Service Worker caching strategies (stale-while-revalidate, cache-first), background sync, and offline capabilities.
  Use when building installable PWAs with offline support.
---

# Progressive Web App (PWA) & Service Worker Guide
- Web App Manifest (`manifest.json`) for installable mobile/desktop home screen icons.
- Workbox Service Worker caching strategies (`StaleWhileRevalidate`, `CacheFirst`).
- Offline fallback pages and push notification subscriptions.
